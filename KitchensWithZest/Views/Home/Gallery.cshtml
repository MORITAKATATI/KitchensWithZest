
@model IEnumerable<KitchensWithZest.Models.Gallery>


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MainPhotoPath)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.PhotoPath)
            </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <img src="@Url.Content(item.MainPhotoPath)" width="50" />
            </td>
         
            <td>
                @Html.ActionLink("Details", "GalleryDetails", new { id = item.GalleryId }) 
            </td>
        </tr>
    }

</table>
